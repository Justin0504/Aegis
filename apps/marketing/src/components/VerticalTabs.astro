---
/**
 * VerticalTabs.astro
 *
 * Hitachi-Vantara-style vertical/category selector — a horizontal tab
 * strip where the active item is marked with a small red dot to the
 * left of the label plus an underline. Below the strip is a card
 * whose contents swap per tab.
 *
 * Four AEGIS use cases: Fintech · Healthcare · Stablecoin · GenAI.
 * Content per panel is real: a headline, a stat pair, a short policy
 * snippet in AEGIS DSL, and a link into the matching blog article.
 *
 * Interaction: keyboard-navigable (arrow keys move focus, Enter/Space
 * activates). No framework — vanilla JS, ~40 lines.
 */

interface Brand {
  name:  string;
  color: string;
  path:  string;             // Simple Icons SVG path (24×24 viewBox)
}
interface Panel {
  key:      string;
  label:    string;
  headline: string;
  lede:     string;
  stats:    [string, string][];
  policy:   string;
  policyMini: string;        // Compact 4-line yaml, rendered under the CTA
  readMore: { href: string; text: string };
  photo:    string;          // Unsplash photo id (part after "photo-")
  photoAlt: string;
  brands:   Brand[];         // Real companies whose tools/APIs the vertical agent calls
}

// Real brand-mark SVG paths (Simple Icons, CC0). Grouped by vertical
// so the tab panels can honestly say "the stack these agents call".
const B = {
  stripe:      { name: 'Stripe',      color: '#635BFF', path: 'M13.479 9.883c-1.626-.604-2.512-1.067-2.512-1.803 0-.622.511-.978 1.422-.978 1.667 0 3.379.642 4.558 1.22l.666-4.111c-.935-.446-2.847-1.177-5.49-1.177-1.87 0-3.425.489-4.536 1.401-1.155.954-1.757 2.334-1.757 4 0 3.023 1.847 4.315 4.842 5.401 1.936.69 2.59 1.18 2.59 1.928 0 .735-.62 1.158-1.766 1.158-1.46 0-3.864-.71-5.444-1.624l-.677 4.16c1.358.768 3.867 1.535 6.475 1.535 1.978 0 3.624-.467 4.74-1.345 1.244-.98 1.889-2.42 1.889-4.275 0-3.087-1.857-4.371-4.943-5.481z' },
  visa:        { name: 'Visa',        color: '#1A1F71', path: 'M9.112 8.262L5.97 15.758H3.92L2.374 9.775c-.094-.368-.175-.503-.461-.658C1.447 8.864.677 8.627 0 8.479l.046-.217h3.3a.904.904 0 01.894.764l.817 4.338 2.018-5.102zm8.033 5.049c.008-1.979-2.736-2.088-2.717-2.972.006-.269.262-.555.822-.628a3.66 3.66 0 011.913.336l.34-1.59a5.207 5.207 0 00-1.814-.333c-1.917 0-3.266 1.02-3.278 2.479-.012 1.079.963 1.68 1.698 2.04.756.367 1.01.603 1.006.931-.005.504-.602.725-1.16.734-.975.015-1.54-.263-1.992-.473l-.351 1.642c.453.208 1.289.39 2.156.398 2.037 0 3.37-1.006 3.377-2.564m5.061 2.447H24l-1.565-7.496h-1.656a.883.883 0 00-.826.55l-2.909 6.946h2.036l.405-1.12h2.488zm-2.163-2.656l1.02-2.815.588 2.815zm-8.16-4.84l-1.603 7.496H8.34l1.605-7.496z' },
  mastercard:  { name: 'Mastercard',  color: '#EB001B', path: 'M11.343 18.031c.058.049.12.098.181.146-1.177.783-2.59 1.238-4.107 1.238C3.32 19.416 0 16.096 0 12c0-4.095 3.32-7.416 7.416-7.416 1.518 0 2.931.456 4.105 1.238-.06.051-.12.098-.165.15C9.6 7.489 8.595 9.688 8.595 12c0 2.311 1.001 4.51 2.748 6.031zm5.241-13.447c-1.52 0-2.931.456-4.105 1.238.06.051.12.098.165.15C14.4 7.489 15.405 9.688 15.405 12c0 2.31-1.001 4.507-2.748 6.031-.058.049-.12.098-.181.146 1.177.783 2.588 1.238 4.107 1.238C20.68 19.416 24 16.096 24 12c0-4.094-3.32-7.416-7.416-7.416zM12 6.174c-.096.075-.189.15-.28.231C10.156 7.764 9.169 9.765 9.169 12c0 2.236.987 4.236 2.551 5.595.09.08.185.158.28.232.096-.074.189-.152.28-.232 1.563-1.359 2.551-3.359 2.551-5.595 0-2.235-.987-4.236-2.551-5.595-.09-.08-.184-.156-.28-.231z' },
  brex:        { name: 'Brex',        color: '#0F0F0F', path: 'M18.69 2.319a3.868 3.868 0 0 0-3.108 1.547l-.759 1.007a1.658 1.658 0 0 1-1.313.656H0V21.68h5.296a3.87 3.87 0 0 0 3.108-1.547l.759-1.006a1.656 1.656 0 0 1 1.313-.657H24V2.319h-5.31Zm1.108 11.949h-5.66a3.87 3.87 0 0 0-3.108 1.547l-.759 1.007a1.658 1.658 0 0 1-1.313.656H4.202V9.731h5.661a3.868 3.868 0 0 0 3.107-1.547l.759-1.006a1.658 1.658 0 0 1 1.313-.657h4.771l-.015 7.747Z' },
  plaid:       { name: 'Plaid',       color: '#111111', path: 'M2 4h4v16H2zm6-1h4v18H8zm6 4h4v10h-4zm6 3h4v4h-4z' },
  circle:      { name: 'Circle',      color: '#0052FF', path: 'M20.788 3.832c-.101-.105-.197-.213-.301-.317-.103-.103-.211-.202-.32-.302A11.903 11.903 0 0 0 12 0a11.926 11.926 0 0 0-8.486 3.514C-1.062 8.09-1.16 15.47 3.213 20.168c.099.108.197.214.3.32.104.103.21.2.317.3A11.92 11.92 0 0 0 12 24c3.206 0 6.22-1.247 8.487-3.512 4.576-4.576 4.673-11.956.301-16.656zm-16.655.301A11.057 11.057 0 0 1 12 .874c2.825 0 5.49 1.048 7.55 2.958l-1.001 1.002A9.646 9.646 0 0 0 12 2.292a9.644 9.644 0 0 0-6.865 2.844A9.644 9.644 0 0 0 2.292 12c0 2.448.9 4.753 2.542 6.549L3.831 19.55C-.201 15.191-.101 8.367 4.133 4.133zm13.798 1.318v.002l-1.015 1.014A7.346 7.346 0 0 0 12 4.589 7.357 7.357 0 0 0 6.761 6.76 7.362 7.362 0 0 0 4.589 12a7.34 7.34 0 0 0 1.877 4.913l-1.014 1.016A8.77 8.77 0 0 1 3.167 12a8.77 8.77 0 0 1 2.588-6.245A8.771 8.771 0 0 1 12 3.167c2.213 0 4.301.809 5.931 2.284zM18.537 12c0 1.745-.681 3.387-1.916 4.622S13.746 18.538 12 18.538a6.491 6.491 0 0 1-4.296-1.621l-.001-.004c-.11-.094-.22-.188-.324-.291a6.027 6.027 0 0 1-.293-.326A6.47 6.47 0 0 1 5.466 12c0-1.746.679-3.387 1.914-4.621A6.488 6.488 0 0 1 12 5.465c1.599 0 3.105.576 4.295 1.62.111.096.224.19.326.295.104.104.2.214.295.324A6.482 6.482 0 0 1 18.537 12zM7.084 17.534h.001A7.349 7.349 0 0 0 12 19.413a7.35 7.35 0 0 0 5.239-2.174A7.354 7.354 0 0 0 19.412 12a7.364 7.364 0 0 0-1.876-4.916l1.013-1.012A8.777 8.777 0 0 1 20.834 12a8.765 8.765 0 0 1-2.589 6.246A8.764 8.764 0 0 1 12 20.834a8.782 8.782 0 0 1-5.93-2.285l1.014-1.015zm12.783 2.333A11.046 11.046 0 0 1 12 23.125a11.042 11.042 0 0 1-7.551-2.957l1.004-1.001a9.64 9.64 0 0 0 6.549 2.542 9.639 9.639 0 0 0 6.865-2.846A9.642 9.642 0 0 0 21.71 12a9.64 9.64 0 0 0-2.543-6.548l1.001-1.002c4.031 4.359 3.935 11.182-.301 15.417z' },
  coinbase:    { name: 'Coinbase',    color: '#0052FF', path: 'M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm-.071 16.643c-2.561 0-4.643-2.082-4.643-4.643s2.082-4.643 4.643-4.643c2.279 0 4.171 1.65 4.566 3.819H21c-.428-4.564-4.275-8.105-8.952-8.105-4.964 0-8.99 4.025-8.99 8.99 0 4.964 4.026 8.99 8.99 8.99 4.677 0 8.524-3.541 8.952-8.105h-4.505c-.395 2.169-2.287 3.697-4.566 3.697z' },
  ethereum:    { name: 'Ethereum',    color: '#3C3C3D', path: 'M11.944 17.97L4.58 13.62 11.943 24l7.37-10.38-7.372 4.35h.003zM12.056 0L4.69 12.223l7.365 4.354 7.365-4.35L12.056 0z' },
  solana:      { name: 'Solana',      color: '#9945FF', path: 'M4.317 15.31c.153-.153.362-.242.582-.242h17.828c.363 0 .544.44.288.696l-3.522 3.518a.822.822 0 01-.582.242H1.083c-.363 0-.544-.44-.288-.696l3.522-3.518zm0-9.788c.153-.153.362-.242.582-.242h17.828c.363 0 .544.44.288.696l-3.522 3.518a.822.822 0 01-.582.242H1.083c-.363 0-.544-.44-.288-.696l3.522-3.518zm14.612 4.882a.822.822 0 00-.582-.242H.519c-.363 0-.544.44-.288.696l3.522 3.518c.153.153.362.242.582.242h17.828c.363 0 .544-.44.288-.696l-3.522-3.518z' },
  anthropic:   { name: 'Anthropic',   color: '#0F0F0F', path: 'M17.3041 3.541h-3.6718l6.696 16.918H24Zm-10.6082 0L0 20.459h3.7442l1.3693-3.5527h7.0052l1.3693 3.5528h3.7442L10.5363 3.5409Zm-.3712 10.2232 2.2914-5.9456 2.2914 5.9456Z' },
  openai:      { name: 'OpenAI',      color: '#10A37F', path: 'M22.282 9.821a5.985 5.985 0 0 0-.516-4.91 6.046 6.046 0 0 0-6.51-2.9A6.065 6.065 0 0 0 4.981 4.18a5.985 5.985 0 0 0-3.998 2.9 6.046 6.046 0 0 0 .743 7.097 5.98 5.98 0 0 0 .51 4.911 6.051 6.051 0 0 0 6.515 2.9A5.985 5.985 0 0 0 13.26 24a6.056 6.056 0 0 0 5.772-4.206 5.99 5.99 0 0 0 3.997-2.9 6.056 6.056 0 0 0-.747-7.073zM13.26 22.43a4.476 4.476 0 0 1-2.876-1.04l.141-.081 4.779-2.758a.795.795 0 0 0 .392-.681v-6.737l2.02 1.168a.071.071 0 0 1 .038.052v5.583a4.504 4.504 0 0 1-4.494 4.494zM3.6 18.304a4.47 4.47 0 0 1-.535-3.014l.142.085 4.783 2.759a.771.771 0 0 0 .78 0l5.843-3.369v2.332a.08.08 0 0 1-.033.062L9.74 19.95a4.5 4.5 0 0 1-6.14-1.646z' },
  gemini:      { name: 'Gemini',      color: '#8E75B2', path: 'M11.04 19.32Q12 21.51 12 24q0-2.49.93-4.68.96-2.19 2.58-3.81t3.81-2.55Q21.51 12 24 12q-2.49 0-4.68-.93a12.3 12.3 0 0 1-3.81-2.58 12.3 12.3 0 0 1-2.58-3.81Q12 2.49 12 0q0 2.49-.96 4.68-.93 2.19-2.55 3.81a12.3 12.3 0 0 1-3.81 2.58Q2.49 12 0 12q2.49 0 4.68.96 2.19.93 3.81 2.55t2.55 3.81' },
  mistral:     { name: 'Mistral',     color: '#FA520F', path: 'M17.143 3.429v3.428h-3.429v3.429h-3.428V6.857H6.857V3.43H3.43v13.714H0v3.428h10.286v-3.428H6.857v-3.429h3.429v3.429h3.429v-3.429h3.428v3.429h-3.428v3.428H24v-3.428h-3.43V3.429z' },
  huggingface: { name: 'Hugging Face', color: '#FFD21E', path: 'M12.025 1.13c-5.77 0-10.449 4.647-10.449 10.378 0 1.112.178 2.181.503 3.185.064-.222.203-.444.416-.577a.96.96 0 0 1 .524-.15c.293 0 .584.124.84.284.278.173.48.408.71.694.226.282.458.611.684.951v-.014c.017-.324.106-.622.264-.874s.403-.487.762-.543c.3-.047.596.06.787.203s.31.313.4.467c.15.257.212.468.233.542.01.026.653 1.552 1.657 2.54.616.605 1.01 1.223 1.082 1.912.055.537-.096 1.059-.38 1.572.637.121 1.294.187 1.967.187.657 0 1.298-.063 1.921-.178-.287-.517-.44-1.041-.384-1.581.07-.69.465-1.307 1.081-1.913 1.004-.987 1.647-2.513 1.657-2.539.021-.074.083-.285.233-.542.09-.154.208-.323.4-.467a1.08 1.08 0 0 1 .787-.203c.359.056.604.29.762.543s.247.55.265.874v.015c.225-.34.457-.67.683-.952.23-.286.432-.52.71-.694.257-.16.547-.284.84-.285a.97.97 0 0 1 .524.151c.228.143.373.388.43.625l.006.04a10.3 10.3 0 0 0 .534-3.273c0-5.731-4.678-10.378-10.449-10.378M8.327 6.583a1.5 1.5 0 0 1 .713.174 1.487 1.487 0 0 1 .617 2.013c-.183.343-.762-.214-1.102-.094-.38.134-.532.914-.917.71a1.487 1.487 0 0 1 .69-2.803m7.486 0a1.487 1.487 0 0 1 .689 2.803c-.385.204-.536-.576-.916-.71-.34-.12-.92.437-1.103.094a1.487 1.487 0 0 1 .617-2.013 1.5 1.5 0 0 1 .713-.174m-10.68 1.55a.96.96 0 1 1 0 1.921.96.96 0 0 1 0-1.92m13.838 0a.96.96 0 1 1 0 1.92.96.96 0 0 1 0-1.92' },
  langchain:   { name: 'LangChain',   color: '#1C3C3C', path: 'M13.796 0a6.93 6.93 0 0 0-4.91 2.019L5.451 5.455l3.273 3.27 3.432-3.432a2.284 2.284 0 0 1 3.277 0 2.28 2.28 0 0 1 0 3.275L12 12.001l3.273 3.273 3.433-3.435c2.692-2.692 2.692-7.127 0-9.82A6.92 6.92 0 0 0 13.796 0m-5.07 8.728-3.433 3.434c-2.692 2.693-2.692 7.126 0 9.819A6.92 6.92 0 0 0 10.203 24a6.93 6.93 0 0 0 4.911-2.02l3.432-3.432-3.271-3.272-3.433 3.433a2.284 2.284 0 0 1-3.277 0 2.28 2.28 0 0 1 0-3.276L12 12z' },
  oracle:      { name: 'Oracle',      color: '#F80000', path: 'M16.412 4.412H7.59A7.588 7.588 0 0 0 0 12a7.588 7.588 0 0 0 7.59 7.588h8.82A7.588 7.588 0 0 0 24 12a7.588 7.588 0 0 0-7.588-7.588m-.176 12.61H7.764a5.023 5.023 0 0 1-5.024-5.022 5.023 5.023 0 0 1 5.024-5.023h8.472A5.023 5.023 0 0 1 21.26 12a5.023 5.023 0 0 1-5.024 5.023' },
  microsoft:   { name: 'Microsoft',   color: '#5E5E5E', path: 'M11.4 24H0V12.6h11.4V24zM24 24H12.6V12.6H24V24zM11.4 11.4H0V0h11.4v11.4zm12.6 0H12.6V0H24v11.4z' },
  google:      { name: 'Google',      color: '#4285F4', path: 'M12.48 10.92v3.28h7.84c-.24 1.84-.853 3.187-1.787 4.133-1.147 1.147-2.933 2.4-6.053 2.4-4.827 0-8.6-3.893-8.6-8.72s3.773-8.72 8.6-8.72c2.6 0 4.507 1.027 5.907 2.347l2.307-2.307C18.747 1.44 16.133 0 12.48 0 5.867 0 .307 5.387.307 12s5.56 12 12.173 12c3.573 0 6.267-1.173 8.373-3.36 2.16-2.16 2.84-5.213 2.84-7.667 0-.76-.053-1.467-.173-2.053H12.48z' },
  apple:       { name: 'Apple',       color: '#000000', path: 'M17.05 20.28c-.98.95-2.05.8-3.08.35-1.09-.46-2.09-.48-3.24 0-1.44.62-2.2.44-3.06-.35C2.79 15.25 3.51 7.59 9.05 7.31c1.35.07 2.29.74 3.08.8 1.18-.24 2.31-.93 3.57-.84 1.51.12 2.65.72 3.4 1.8-3.12 1.87-2.38 5.98.48 7.13-.57 1.5-1.31 2.99-2.54 4.09zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z' },
  aws:         { name: 'AWS',         color: '#FF9900', path: 'M6.763 10.036c0 .296.032.535.088.71.064.176.144.368.256.576.04.063.056.127.056.183 0 .08-.048.16-.152.24l-.503.335a.383.383 0 0 1-.208.072c-.08 0-.16-.04-.24-.112a2.47 2.47 0 0 1-.288-.375 6.18 6.18 0 0 1-.248-.471c-.622.734-1.405 1.101-2.347 1.101-.67 0-1.205-.191-1.596-.574-.391-.384-.59-.894-.59-1.533 0-.678.238-1.23.717-1.644.478-.415 1.116-.623 1.928-.623.27 0 .55.024.846.064.295.04.599.104.918.176v-.583c0-.607-.127-1.03-.375-1.277-.255-.248-.686-.367-1.3-.367-.28 0-.567.031-.862.103a6.373 6.373 0 0 0-1.212.437.323.323 0 0 1-.12.04c-.08 0-.144-.023-.184-.087-.048-.056-.072-.112-.072-.184v-.383c0-.088.016-.152.048-.184.032-.032.088-.064.16-.096.28-.144.622-.272 1.006-.383a4.83 4.83 0 0 1 1.269-.176c.966 0 1.68.223 2.135.663.454.44.68 1.111.68 2.014V10.036zM3.076 11.42c.263 0 .534-.048.822-.144.287-.096.542-.271.758-.51.128-.152.223-.32.279-.512.056-.191.088-.423.088-.694v-.335a6.66 6.66 0 0 0-.734-.136 6.007 6.007 0 0 0-.75-.048c-.535 0-.926.104-1.19.32-.263.215-.39.518-.39.917 0 .375.095.655.295.846.191.2.47.296.822.296zm7.294.982c-.16 0-.263-.024-.335-.088-.072-.056-.135-.183-.19-.359L7.72 5.116a1.607 1.607 0 0 1-.08-.375c0-.152.072-.24.216-.24h.782c.167 0 .278.024.335.087.08.056.135.183.183.36l1.554 6.144L12.15 4.948c.048-.176.104-.303.183-.359a.694.694 0 0 1 .344-.088h.638c.167 0 .279.024.335.087.08.056.135.183.183.36l1.451 6.223 1.601-6.223c.056-.176.112-.303.183-.36.08-.055.184-.087.335-.087h.744c.144 0 .223.072.223.24 0 .048-.008.104-.024.16a1.407 1.407 0 0 1-.056.24l-2.238 7.144c-.048.176-.112.303-.183.359a.55.55 0 0 1-.335.088h-.686c-.167 0-.279-.024-.335-.088-.08-.056-.135-.183-.183-.359l-1.442-5.974-1.435 5.966c-.048.176-.104.303-.183.36a.694.694 0 0 1-.335.087zm11.673.216a5.208 5.208 0 0 1-1.212-.14 3.593 3.593 0 0 1-.902-.319c-.16-.088-.263-.184-.303-.263a.685.685 0 0 1-.056-.264v-.399c0-.16.056-.24.176-.24a.446.446 0 0 1 .144.024c.048.016.12.048.199.088.263.112.55.199.854.263.312.056.615.088.926.088.487 0 .87-.088 1.14-.263.256-.176.39-.423.39-.75 0-.223-.072-.407-.216-.559-.144-.152-.415-.288-.806-.415l-1.156-.359c-.583-.184-1.014-.455-1.277-.815a1.907 1.907 0 0 1-.399-1.157c0-.334.072-.63.216-.886.144-.256.336-.479.575-.655.24-.183.518-.32.83-.415.318-.096.65-.144.997-.144a4.17 4.17 0 0 1 .59.048c.176.024.336.056.487.088.144.04.28.087.407.135.128.048.223.096.28.144.079.048.135.096.167.16a.462.462 0 0 1 .04.208v.367c0 .16-.057.24-.176.24-.064 0-.176-.032-.32-.096a3.844 3.844 0 0 0-1.6-.32c-.44 0-.79.072-1.03.223-.239.144-.359.375-.359.695 0 .223.088.407.24.559.16.152.446.303.87.44l1.132.355c.575.184.997.44 1.245.782.247.336.375.72.375 1.148 0 .343-.064.655-.191.926-.128.28-.311.51-.55.703-.24.191-.527.335-.855.446-.343.104-.703.16-1.109.16zm.79 4.104C19.5 18.65 15.917 20 12.75 20 8.31 20 4.307 18.36 1.253 15.626c-.24-.216-.024-.512.263-.343 3.294 1.916 7.362 3.076 11.573 3.076 2.815 0 5.909-.583 8.757-1.788.423-.184.782.28.367.583zm1.166-1.396c-.288-.367-1.907-.176-2.634-.088-.223.024-.263-.16-.056-.303 1.29-.902 3.412-.647 3.66-.343.248.311-.063 2.433-1.276 3.447-.183.152-.36.072-.28-.128.28-.678.902-2.207.582-2.583z' },
  aegis_gray:  { name: 'AEGIS gates every one', color: '#5c5a50', path: '' },   // sentinel for text-only chip
} as const;

const PANELS: Panel[] = [
  {
    key:   'stablecoin',
    label: 'Stablecoin',
    headline: 'Treasury agents that can\'t drain the wallet.',
    lede: 'FATF Travel Rule metadata, wallet allowlist, 2-of-N approval on high-value transfers — enforced at the tool-call layer, before the transaction hits the mempool.',
    stats: [
      ['$1,000', 'Travel Rule threshold: automatic originator + beneficiary attachment'],
      ['2-of-N', 'high-value approval enforced deterministically, not by prompt'],
    ],
    policy: `rule: "stablecoin-egress-2of2"
when:
  - tool.name == "circle_usdc.transfer"
  - amount > 10_000_00
  - wallet NOT IN treasury.allowlist
require:
  approvers: 2
  scope:     "finance-ops"
action: ESCALATE`,
    readMore: { href: '/blog/stablecoin-agent-security-travel-rule', text: 'Read the Travel Rule playbook' },
    policyMini: `rule:    "stablecoin-egress-2of2"
when:    amount > 10_000_00
require: wallet IN treasury.allowlist
action:  ESCALATE`,
    photo:    '1639762681485-074b7f938ba0',
    photoAlt: 'Abstract cryptocurrency chain visualisation, warm gold tones',
    brands:   [B.circle, B.coinbase, B.ethereum, B.solana, B.stripe],
  },
  {
    key:   'fintech',
    label: 'Fintech',
    headline: 'Refund agents that survive a PCI-DSS audit.',
    lede: 'Tokenise cardholder data at the gateway, gate high-value refunds behind 2-of-N approval, and ship a tamper-evident evidence pack the auditor accepts.',
    stats: [
      ['80%',  'of a compliance review answered by Req 10 + Req 8 + SOC 2 CC6.1/CC7.2/CC8.1'],
      ['27m',  'median time from install to first audited tool call'],
    ],
    policy: `rule: "refund-high-value-2of2"
when:
  - tool.name == "stripe.refund"
  - amount > 1_000_00
require:
  approvers: 2
  scope:     "finance-ops"
action: ESCALATE`,
    readMore: { href: '/blog/ai-agent-safety-fintech-pci-dss', text: 'Read the PCI-DSS playbook' },
    policyMini: `rule:    "refund-high-value-2of2"
when:    amount > 1_000_00
require: approvers: 2, scope: "finance-ops"
action:  ESCALATE`,
    photo:    '1620712943543-bcc4688e7485',
    photoAlt: 'Abstract AI / neural network visualisation — same pick as the AI-agent-runtime blog cover',
    brands:   [B.stripe, B.visa, B.mastercard, B.plaid, B.brex],
  },
  {
    key:   'healthcare',
    label: 'Healthcare',
    headline: 'PHI-safe agents by construction.',
    lede: 'Every tool call is gated against a HIPAA §164.312 checklist. PHI is tokenised at ingress; audit controls are tamper-evident by default.',
    stats: [
      ['7',    'HIPAA Security Rule requirements mapped to tool-call policy'],
      ['3',    'addressable specs that become required the moment an agent touches PHI'],
    ],
    policy: `rule: "phi-egress-baa-only"
when:
  - tool.name == "fhir.write"
  - arg.subject_ref.contains("Patient/")
require:
  destination_baa: true
  min_scope:       "chart-note"
action: BLOCK`,
    readMore: { href: '/blog/hipaa-compliant-ai-agents', text: 'Read the HIPAA playbook' },
    policyMini: `rule:    "phi-egress-baa-only"
when:    tool.name == "fhir.write"
require: destination_baa: true
action:  BLOCK`,
    photo:    '1576091160550-2173dba999ef',
    photoAlt: 'Medical AI imagery — same pick as the HIPAA-compliant-agents blog cover',
    // Cloud + EMR platforms that host PHI-touching healthcare agents.
    brands:   [B.oracle, B.microsoft, B.google, B.apple, B.aws],
  },
  {
    key:   'genai',
    label: 'GenAI',
    headline: 'The runtime safety layer for any agent stack.',
    lede: 'Prompt injection, indirect injection via tool outputs, tool-arg exfiltration — every attack surface is a rule + a taint label + a calibrated judge, in that order.',
    stats: [
      ['0.69%',  'attack success rate under parameter-level taint (IPIGuard, EMNLP 2025)'],
      ['3',      'layers: deterministic policy → behavioural anomaly → calibrated judge'],
    ],
    policy: `rule: "tool-output-taint-strict"
when:
  - tool.arg.origin IN ["web", "email", "pdf"]
  - target.sink IN ["send_email", "transfer", "file.write"]
action: BLOCK
reason: "untrusted-source-to-sink"`,
    readMore: { href: '/blog/what-is-ai-agent-runtime-safety', text: 'Read the runtime safety guide' },
    policyMini: `rule:    "tool-output-taint-strict"
when:    arg.origin IN ["web","email","pdf"]
require: sink NOT IN sensitive
action:  BLOCK`,
    photo:    '1518770660439-4636190af475',
    photoAlt: 'Compute infrastructure close-up — GPU / motherboard detail',
    brands:   [B.anthropic, B.openai, B.gemini, B.mistral, B.langchain, B.huggingface],
  },
];
---

<section class="vt" aria-labelledby="vt-heading">
  <div class="container">
    <p class="vt-eyebrow">Deploy across verticals</p>
    <h2 id="vt-heading">Runtime safety, tuned to your regulator.</h2>

    <div class="vt-tablist" role="tablist" aria-label="AEGIS vertical use cases">
      {PANELS.map((p, i) => (
        <button
          type="button"
          role="tab"
          id={`vt-tab-${p.key}`}
          class={`vt-tab ${i === 0 ? 'active' : ''}`}
          aria-selected={i === 0 ? 'true' : 'false'}
          aria-controls={`vt-panel-${p.key}`}
          tabindex={i === 0 ? '0' : '-1'}
          data-tab={p.key}
        >
          <span class="vt-dot" aria-hidden="true"></span>
          <span class="vt-label">{p.label}</span>
        </button>
      ))}
    </div>

    <div class="vt-panels">
      {PANELS.map((p, i) => (
        <article
          role="tabpanel"
          id={`vt-panel-${p.key}`}
          class={`vt-panel ${i === 0 ? 'active' : ''}`}
          aria-labelledby={`vt-tab-${p.key}`}
          hidden={i !== 0}
          data-panel={p.key}
        >
          <div class="vt-grid">
            <div class="vt-copy">
              <div class="vt-copy-top">
                <h3>{p.headline}</h3>
                <p class="vt-lede">{p.lede}</p>
              </div>

              <dl class="vt-stats">
                {p.stats.map(([k, v]) => (
                  <div class="vt-stat">
                    <dt>{k}</dt>
                    <dd>{v}</dd>
                  </div>
                ))}
              </dl>

              <a class="vt-read" href={p.readMore.href}>
                {p.readMore.text}
                <span aria-hidden="true">→</span>
              </a>

              {/* Compact policy snippet beneath the CTA — fills the
                  otherwise-empty lower-left with a concrete artefact
                  from the vertical. No chrome (no dots, no filename),
                  just the yaml at a small monospace size. */}
              <pre class="vt-policy-mini" aria-hidden="true">{p.policyMini}</pre>
            </div>

            <div class="vt-visual" aria-hidden="true">
              <figure class="vt-photo">
                <img
                  src={`https://images.unsplash.com/photo-${p.photo}?w=1000&h=760&q=80&fit=crop&auto=format`}
                  alt={p.photoAlt}
                  width="1000" height="760"
                  loading="lazy" decoding="async"
                />
                <span class={`vt-photo-tint tint-${p.key}`} aria-hidden="true"></span>
              </figure>
            </div>
          </div>

          {/* Full-width brand strip beneath the two-column grid. */}
          <div class="vt-brands">
            <p class="vt-brands-label">Compatible with the stack these agents actually call</p>
            <ul class="vt-brand-row">
              {p.brands.map(b => (
                <li class="vt-brand" title={b.name}>
                  <span class="vt-brand-mark" style={`background:${b.color}`}>
                    <svg viewBox="0 0 24 24" fill="#fff" fill-rule="evenodd" aria-hidden="true"><path d={b.path}/></svg>
                  </span>
                  <span class="vt-brand-name">{b.name}</span>
                </li>
              ))}
            </ul>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

<script is:inline>
  (() => {
    const root = document.currentScript?.previousElementSibling;
    if (!root) return;
    const tabs   = root.querySelectorAll('.vt-tab');
    const panels = root.querySelectorAll('.vt-panel');

    function activate(key) {
      tabs.forEach(t => {
        const on = t.dataset.tab === key;
        t.classList.toggle('active', on);
        t.setAttribute('aria-selected', on ? 'true' : 'false');
        t.setAttribute('tabindex', on ? '0' : '-1');
      });
      panels.forEach(p => {
        const on = p.dataset.panel === key;
        p.classList.toggle('active', on);
        if (on) p.removeAttribute('hidden');
        else    p.setAttribute('hidden', '');
      });
    }

    tabs.forEach(t => {
      t.addEventListener('click', () => activate(t.dataset.tab));
    });

    // Arrow-key navigation on the tablist.
    root.querySelector('.vt-tablist')?.addEventListener('keydown', ev => {
      if (!['ArrowLeft', 'ArrowRight', 'Home', 'End'].includes(ev.key)) return;
      ev.preventDefault();
      const list = Array.from(tabs);
      const cur  = list.findIndex(t => t.classList.contains('active'));
      let next = cur;
      if (ev.key === 'ArrowLeft')  next = (cur - 1 + list.length) % list.length;
      if (ev.key === 'ArrowRight') next = (cur + 1) % list.length;
      if (ev.key === 'Home')       next = 0;
      if (ev.key === 'End')        next = list.length - 1;
      activate(list[next].dataset.tab);
      list[next].focus();
    });
  })();
</script>

<style>
  .vt {
    padding: 6rem 0 5rem;
    background: var(--bg);
    border-top: 1px solid var(--hairline);
  }
  /* Panel gets an inner container-narrow-esque max so it doesn't
     stretch to full-bleed on wide screens, but still uses more than
     the read-optimised text column. */
  .vt .container { max-width: 1180px; }
  .vt-eyebrow {
    text-align: center;
    font-size: 0.82rem;
    letter-spacing: 0.06em;
    color: var(--muted);
    font-weight: 500;
    margin: 0 0 0.6rem;
  }
  .vt h2 {
    text-align: center;
    font-family: var(--font-serif);
    font-size: clamp(1.9rem, 3.2vw, 2.4rem);
    letter-spacing: -0.02em;
    margin: 0 0 2.5rem;
    line-height: 1.15;
  }

  /* ── Tablist ─────────────────────────────────────────────────────── */
  .vt-tablist {
    display: flex;
    gap: 2.4rem;
    justify-content: center;
    flex-wrap: wrap;
    margin: 0 0 2rem;
    border-bottom: 1px solid var(--hairline);
  }
  .vt-tab {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.85rem 0.2rem 1rem;
    /* Reset every button default so the tab reads as plain text —
       no rounded button box, no browser focus rectangle. */
    background: none;
    border: none;
    outline: none;
    appearance: none;
    -webkit-appearance: none;
    -webkit-tap-highlight-color: transparent;
    color: var(--muted);
    font-family: var(--font-sans);
    font-size: 1.02rem;
    font-weight: 500;
    letter-spacing: -0.005em;
    cursor: pointer;
    transition: color 0.18s ease;
  }
  .vt-tab:hover { color: var(--text); }
  /* Keyboard-only focus: soft dashed ring, offset well clear of the
     underline so the two don't collide. Mouse click never triggers. */
  .vt-tab:focus-visible {
    outline: 2px dashed var(--hairline-strong);
    outline-offset: 6px;
    border-radius: 4px;
  }

  /* Red dot on the left, only rendered on active. Uses the accent-warn
     sand-red so it reads as an AEGIS-palette indicator, not a generic
     alert. */
  .vt-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: transparent;
    transition: background 0.18s ease, transform 0.18s ease;
    flex-shrink: 0;
  }
  .vt-tab.active {
    color: var(--text);
  }
  .vt-tab.active .vt-dot {
    background: #b04a3a;
    transform: scale(1.15);
    box-shadow: 0 0 0 3px rgba(176, 74, 58, 0.16);
  }

  /* Active tab underline — offset a hair below the label. */
  .vt-tab::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: -1px;
    height: 2px;
    background: var(--text);
    transform: scaleX(0);
    transform-origin: center;
    transition: transform 0.22s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .vt-tab.active::after { transform: scaleX(1); }

  /* ── Panels ──────────────────────────────────────────────────────── */
  .vt-panels {
    position: relative;
  }
  /* Each panel is one unified card — subtle warm bg, thin hairline,
     soft shadow. Content inside is a two-column grid on top of a
     full-width brand strip. Tighter padding + gap so the panel reads
     as a dense editorial card, not a spacious splash. */
  .vt-panel {
    display: flex;
    flex-direction: column;
    gap: 1.1rem;
    padding: 1.4rem 1.6rem 1.5rem;
    margin-top: 1rem;
    background: var(--surface);
    border: 1px solid var(--hairline);
    border-radius: 18px;
    box-shadow:
      0 24px 60px rgba(10, 10, 10, 0.05),
      0 2px 6px rgba(10, 10, 10, 0.03);
    opacity: 0;
    transform: translateY(6px);
    animation: vt-in 0.35s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }
  .vt-panel[hidden] { display: none; }
  @keyframes vt-in {
    to { opacity: 1; transform: translateY(0); }
  }

  /* Two-column grid. No min-height — the panel sizes to its content
     rather than to an arbitrary floor. */
  .vt-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.05fr);
    gap: 1.8rem;
    align-items: stretch;
  }

  /* Left column — content clusters at the top; the CTA sits directly
     under the stats (no `space-between` stretch). */
  .vt-copy {
    display: flex;
    flex-direction: column;
    padding: 0.2rem 0;
  }
  .vt-copy h3 {
    font-family: var(--font-serif);
    font-size: clamp(1.35rem, 2.2vw, 1.7rem);
    letter-spacing: -0.015em;
    margin: 0 0 0.6rem;
    line-height: 1.18;
    max-width: 24ch;
  }
  .vt-lede {
    color: var(--muted);
    font-size: 0.94rem;
    line-height: 1.55;
    margin: 0;
    max-width: 46ch;
  }

  /* Stats — two horizontal blocks separated by a hairline instead of
     a naive grid. Compact so the panel breathes. */
  .vt-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    margin: 1.1rem 0 1rem;
    padding: 0.85rem 0;
    border-top: 1px solid var(--hairline);
    border-bottom: 1px solid var(--hairline);
  }
  .vt-stat {
    padding-right: 1rem;
  }
  .vt-stat + .vt-stat {
    padding-right: 0;
    padding-left: 1rem;
    border-left: 1px solid var(--hairline);
  }
  .vt-stat dt {
    font-family: var(--font-mono, ui-monospace);
    font-size: 1.35rem;
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--text);
    line-height: 1;
    margin: 0 0 0.3rem;
    font-variant-numeric: tabular-nums;
  }
  .vt-stat dd {
    margin: 0;
    color: var(--muted);
    font-size: 0.76rem;
    line-height: 1.45;
    max-width: 24ch;
  }

  .vt-read {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    color: var(--text);
    text-decoration: none;
    font-weight: 500;
    font-size: 0.9rem;
    padding-bottom: 0.15rem;
    border-bottom: 1px solid var(--hairline-strong);
    transition: gap 0.18s ease, border-color 0.18s ease;
    align-self: flex-start;
    margin-top: 0.2rem;
  }
  .vt-read:hover {
    gap: 0.55rem;
    border-bottom-color: var(--text);
  }

  /* Compact policy snippet — warm sand surface (matches the panel's
     painted-canvas palette, no stark black rectangle), monospace text
     in the site's ink colour, a soft left accent strip so it reads
     as "quoted config" rather than "terminal window". */
  .vt-policy-mini {
    margin: 1.2rem 0 0;
    padding: 0.7rem 0.9rem 0.75rem 1rem;
    background: var(--surface-2);
    color: var(--text);
    border-left: 3px solid var(--hairline-strong);
    border-radius: 4px;
    font-family: var(--font-mono, ui-monospace);
    font-size: 0.72rem;
    line-height: 1.6;
    white-space: pre;
    overflow-x: auto;
    box-shadow: 0 1px 2px rgba(10, 10, 10, 0.04);
  }

  /* ── Right column: photo only. Fills the column, matches the copy
        column's natural height via `align-items: stretch` on the grid. ── */
  .vt-visual {
    display: flex;
  }
  .vt-photo {
    margin: 0;
    position: relative;
    flex: 1;
    border-radius: 12px;
    overflow: hidden;
    box-shadow:
      0 14px 34px rgba(10, 10, 10, 0.12),
      0 2px 6px rgba(10, 10, 10, 0.06);
    background: var(--surface-2);
    min-height: 240px;
  }
  .vt-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  /* Warm painted-canvas tint per vertical — keeps every photo on-brand
     against the sand background. */
  .vt-photo-tint {
    position: absolute;
    inset: 0;
    pointer-events: none;
    mix-blend-mode: multiply;
    opacity: 0.38;
  }
  .tint-fintech    { background: linear-gradient(135deg, #635BFF 0%, transparent 55%), linear-gradient(225deg, #b04a3a 0%, transparent 60%); }
  .tint-healthcare { background: linear-gradient(135deg, #B7C8C9 0%, transparent 55%), linear-gradient(225deg, #6B7E7F 0%, transparent 60%); }
  .tint-stablecoin { background: linear-gradient(135deg, #D4B5A8 0%, transparent 55%), linear-gradient(225deg, #7B8B7E 0%, transparent 60%); }
  .tint-genai      { background: linear-gradient(135deg, #A4B3A0 0%, transparent 55%), linear-gradient(225deg, #8B6F60 0%, transparent 60%); }

  /* ── Full-width brand strip below the two-column grid ──────────── */
  .vt-brands {
    margin-top: 0.2rem;
    padding-top: 1rem;
    border-top: 1px solid var(--hairline);
    display: flex;
    align-items: center;
    gap: 1rem 1.2rem;
    flex-wrap: wrap;
  }
  .vt-brands-label {
    font-size: 0.68rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--muted);
    margin: 0;
    font-weight: 600;
    flex-shrink: 0;
  }
  .vt-brand-row {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 0.7rem;
    align-items: center;
  }
  .vt-brand {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.25rem 0.55rem 0.25rem 0.3rem;
    background: var(--bg);
    border: 1px solid var(--hairline);
    border-radius: 999px;
    transition: transform 0.15s ease, border-color 0.15s ease;
  }
  .vt-brand:hover {
    transform: translateY(-1px);
    border-color: var(--hairline-strong);
  }
  .vt-brand-mark {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    box-shadow: 0 1px 2px rgba(0,0,0,0.10);
  }
  .vt-brand-mark svg {
    width: 11px;
    height: 11px;
  }
  .vt-brand-name {
    font-size: 0.78rem;
    color: var(--text);
    font-weight: 500;
    letter-spacing: -0.005em;
  }
  /* Code card sits directly below the photo — natural height, no
     forced flex-grow. Reads as a compact companion artefact. */
  .vt-code {
    width: 100%;
    background: #1a1816;
    border-radius: 12px;
    overflow: hidden;
    box-shadow:
      0 12px 30px rgba(10, 10, 10, 0.16),
      0 2px 6px rgba(10, 10, 10, 0.08);
  }
  .vt-code-bar {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.45rem 0.75rem;
    background: #242220;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
  }
  .vt-code-bar > span:not(.vt-code-file) {
    width: 9px;
    height: 9px;
    border-radius: 50%;
  }
  .vt-dot-red { background: #ff5f57; }
  .vt-dot-yel { background: #febc2e; }
  .vt-dot-grn { background: #28c840; }
  .vt-code-file {
    margin-left: 0.4rem;
    color: rgba(216, 211, 192, 0.55);
    font-family: var(--font-mono, ui-monospace);
    font-size: 0.72rem;
  }
  /* Explicit background on the <pre> — a global `pre { background:
     surface }` rule elsewhere was leaking a light color into the
     panel, so the code appeared washed out. Explicit dark bg + bright
     text keeps this panel readable regardless of the parent scope.
     Smaller font-size + tighter padding so the whole yaml fits inside
     the panel without horizontal clip. */
  .vt-code-body {
    margin: 0;
    padding: 0.75rem 1rem 0.85rem;
    background: #1a1816;
    color: #eae3d2;
    font-family: var(--font-mono, ui-monospace);
    font-size: 0.72rem;
    line-height: 1.5;
    overflow-x: auto;
    white-space: pre;
  }
  /* Simple syntax-color shading via nested spans if we ever add them. */
  .vt-code-body :global(code) {
    background: transparent;
    color: inherit;
    padding: 0;
  }

  /* ── Responsive ─────────────────────────────────────────────────── */
  @media (max-width: 900px) {
    .vt-panel { padding: 1.5rem; margin: 0.8rem 0.2rem 0; }
    .vt-grid {
      grid-template-columns: 1fr;
      gap: 1.6rem;
      min-height: auto;
    }
    .vt-copy { padding: 0; }
    .vt-visual {
      min-height: 260px;
      order: -1;                              /* photo on top on mobile */
    }
    .vt-code {
      width: 100%;
    }
    .vt-stats { padding: 0.9rem 0; }
    .vt-stat dt { font-size: 1.5rem; }
    .vt-tablist { gap: 1.4rem; }
    .vt-tab { font-size: 0.94rem; padding: 0.7rem 0.1rem 0.85rem; }
    .vt-code-body { font-size: 0.74rem; padding: 0.8rem 1rem; }
    .vt-brands {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.7rem;
    }
    .vt-brand-name { display: none; }
    .vt-brand { padding: 0.35rem; }
  }

  /* Reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .vt-panel { animation: none; opacity: 1; transform: none; }
    .vt-tab, .vt-tab::after, .vt-dot { transition: none; }
  }
</style>
