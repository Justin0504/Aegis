---
import Layout from '@/layouts/Layout.astro';
import { getPublicSupabaseUrl, getPublicSupabaseAnonKey } from '@/lib/supabase';

// If already signed in, jump straight to `next` (or /download by default).
const user = (Astro.locals as any).user;
const nextParam = new URL(Astro.request.url).searchParams.get('next') || '/download';
if (user) return Astro.redirect(nextParam, 302);

const supabaseUrl  = getPublicSupabaseUrl(Astro.locals);
const supabaseAnon = getPublicSupabaseAnonKey(Astro.locals);
const supabaseConfigured = Boolean(supabaseUrl && supabaseAnon);
---
<Layout title="Sign in — AEGIS" description="Sign in to your AEGIS account.">
  <main class="split">
    <aside class="left">
      <a href="/" class="brand" aria-label="AEGIS home">
        <svg width="120" height="26" viewBox="0 0 92 22" fill="none" xmlns="http://www.w3.org/2000/svg">
          <text x="0" y="17" font-family="Inter, ui-sans-serif" font-weight="700" font-size="18" letter-spacing="-0.02em">
            <tspan fill="#E85E14">A</tspan><tspan fill="currentColor">EGIS</tspan>
          </text>
        </svg>
      </a>
      <div class="marquee">
        <div class="col col-a">
          <!-- Testimonials duplicated for seamless loop; keep the two
               copies IDENTICAL so the halfway wrap-point is invisible. -->
          {[...new Array(2)].map(() => (
            <>
              <blockquote class="card">
                <span class="mark">&ldquo;</span>
                <p>AEGIS gives us the audit trail our SOC 2 reviewer was actually looking for — signed, reproducible, in one click.</p>
                <footer><div class="avatar">JM</div><div><div class="who">Jason M.</div><div class="role">Head of Platform · fintech Series C</div></div></footer>
              </blockquote>
              <blockquote class="card">
                <span class="mark">&ldquo;</span>
                <p>Air-gapped Helm chart, Ollama sidecar, EU AI Act evidence pack — the only stack our BFSI security team didn't push back on.</p>
                <footer><div class="avatar">MM</div><div><div class="who">Maurice M.</div><div class="role">CISO · European retail bank</div></div></footer>
              </blockquote>
              <blockquote class="card">
                <span class="mark">&ldquo;</span>
                <p>Cut our agent incident MTTD from hours to seconds. The compensator precedence stuff is <em>chef's kiss</em>.</p>
                <footer><div class="avatar">AK</div><div><div class="who">Aditi K.</div><div class="role">SRE Lead · healthtech Series B</div></div></footer>
              </blockquote>
              <blockquote class="card">
                <span class="mark">&ldquo;</span>
                <p>BFSI DSL pack shipped with GLBA + PCI + SOX rules out of the box. Would've taken us a month to write ourselves.</p>
                <footer><div class="avatar">RB</div><div><div class="who">Rohan B.</div><div class="role">Head of Compliance · payments</div></div></footer>
              </blockquote>
            </>
          ))}
        </div>
        <div class="col col-b">
          {[...new Array(2)].map(() => (
            <>
              <blockquote class="card">
                <span class="mark">&ldquo;</span>
                <p>Workflow-anchored policies mean same tool call, different node — different rule. First runtime firewall that <em>gets</em> agents.</p>
                <footer><div class="avatar">GO</div><div><div class="who">Gostree O.</div><div class="role">Staff Engineer · agentic infra</div></div></footer>
              </blockquote>
              <blockquote class="card">
                <span class="mark">&ldquo;</span>
                <p>Traded 400 lines of guardrail glue for 12 lines of DSL. Reviews caught two prompt-injection vectors on day one.</p>
                <footer><div class="avatar">LP</div><div><div class="who">Léa P.</div><div class="role">Principal AI Engineer · SaaS</div></div></footer>
              </blockquote>
              <blockquote class="card">
                <span class="mark">&ldquo;</span>
                <p>Ollama sidecar + AEGIS_OFFLINE = my CISO stopped asking whether the vendor sees our prompts.</p>
                <footer><div class="avatar">DN</div><div><div class="who">David N.</div><div class="role">VP Eng · defense contractor</div></div></footer>
              </blockquote>
              <blockquote class="card">
                <span class="mark">&ldquo;</span>
                <p>The EU AI Act evidence pack turned a two-week export project into a one-click download. Auditor loved it.</p>
                <footer><div class="avatar">SM</div><div><div class="who">Sofia M.</div><div class="role">GRC Manager · EU insurance</div></div></footer>
              </blockquote>
            </>
          ))}
        </div>
      </div>
    </aside>

    <section class="right">
      <div class="form-wrap">
        <h1>Sign in</h1>
        <p class="sub">Enter your work email below to sign in to your account.</p>

        {!supabaseConfigured && (
          <div class="notice">
            <strong>Auth not configured.</strong> Set <code>PUBLIC_SUPABASE_URL</code> +
            <code>PUBLIC_SUPABASE_ANON_KEY</code> in Cloudflare Pages env. See
            <a href="https://github.com/Justin0504/Aegis/blob/main/docs/AUTH-SETUP.md">docs/AUTH-SETUP.md</a>.
          </div>
        )}

        <form id="magic-form" autocomplete="on">
          <label>
            <span>Email</span>
            <input type="email" name="email" placeholder="user@example.com" required autocomplete="email" />
          </label>
          <button type="submit" class="btn primary" disabled={!supabaseConfigured}>
            Sign in with email
          </button>
        </form>

        <div class="divider"><span>or</span></div>

        <button class="btn oauth" data-provider="google" disabled={!supabaseConfigured}>
          <svg width="18" height="18" viewBox="0 0 48 48" fill="none">
            <path fill="#4285F4" d="M43.6 20.5H42V20H24v8h11.3C33.7 32.9 29.2 36 24 36c-6.6 0-12-5.4-12-12s5.4-12 12-12c3 0 5.7 1.1 7.9 3l5.7-5.7C33.8 6.1 29.1 4 24 4 12.9 4 4 12.9 4 24s8.9 20 20 20 20-8.9 20-20c0-1.2-.1-2.3-.4-3.5z"/>
            <path fill="#34A853" d="M6.3 14.7l6.6 4.8C14.7 15.3 19 12 24 12c3 0 5.7 1.1 7.9 3l5.7-5.7C33.8 6.1 29.1 4 24 4 16.3 4 9.6 8.3 6.3 14.7z"/>
            <path fill="#FBBC05" d="M24 44c5 0 9.6-1.9 13-5l-6-5.1c-1.9 1.3-4.4 2.1-7 2.1-5.2 0-9.6-3.4-11.2-8.1l-6.5 5C9.5 39.6 16.2 44 24 44z"/>
            <path fill="#EA4335" d="M43.6 20.5H42V20H24v8h11.3c-.8 2.3-2.3 4.2-4.3 5.4l6 5.1C40.9 34.6 44 29.7 44 24c0-1.2-.1-2.3-.4-3.5z"/>
          </svg>
          Sign in with Google
        </button>

        <button class="btn oauth" data-provider="github" disabled={!supabaseConfigured}>
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 .3a12 12 0 0 0-3.8 23.4c.6.1.8-.3.8-.6v-2.2c-3.3.7-4-1.4-4-1.4-.5-1.4-1.3-1.8-1.3-1.8-1.1-.8.1-.7.1-.7 1.2.1 1.8 1.2 1.8 1.2 1.1 1.9 2.9 1.3 3.6 1 .1-.8.4-1.3.8-1.6-2.7-.3-5.5-1.3-5.5-6 0-1.3.5-2.4 1.2-3.2-.1-.3-.5-1.5.1-3.2 0 0 1-.3 3.3 1.2a11.5 11.5 0 0 1 6 0C17.3 4.7 18.3 5 18.3 5c.7 1.7.2 2.9.1 3.2.8.8 1.2 1.9 1.2 3.2 0 4.6-2.8 5.6-5.5 5.9.4.4.8 1.1.8 2.2v3.3c0 .3.2.7.8.6A12 12 0 0 0 12 .3"/>
          </svg>
          Sign in with GitHub
        </button>

        <button class="btn oauth" data-provider="linkedin_oidc" disabled={!supabaseConfigured}>
          <svg width="18" height="18" viewBox="0 0 24 24" fill="#0A66C2">
            <path d="M20.4 20.4h-3.6v-5.6c0-1.3 0-3-1.8-3-1.9 0-2.2 1.5-2.2 2.9v5.7H9.3V9h3.4v1.6h.1c.5-.9 1.7-1.8 3.4-1.8 3.6 0 4.3 2.4 4.3 5.5v6.1zM5.3 7.4a2.1 2.1 0 1 1 0-4.2 2.1 2.1 0 0 1 0 4.2zm1.8 13h-3.6V9h3.6v11.4zM22.2 0H1.8C.8 0 0 .8 0 1.8v20.4C0 23.2.8 24 1.8 24h20.4c1 0 1.8-.8 1.8-1.8V1.8C24 .8 23.2 0 22.2 0z"/>
          </svg>
          Sign in with LinkedIn
        </button>

        <p class="check-email" id="check-email" hidden>
          Check your email — we sent a one-time sign-in link to
          <strong id="check-email-addr"></strong>.
        </p>
        <p class="err" id="err" hidden></p>

        <p class="tos">
          By continuing you agree to our
          <a href="/terms">Terms</a> and <a href="/privacy">Privacy Policy</a>.
        </p>
        <p class="signup">
          Don't have an account? <a href={`/signup?next=${encodeURIComponent(nextParam)}`}>Sign up</a>
        </p>
      </div>
    </section>
  </main>

  <script is:inline define:vars={{ supabaseUrl, supabaseAnon, nextParam }}>
    if (supabaseUrl && supabaseAnon) {
      const { createClient } = await import('https://esm.sh/@supabase/supabase-js@2.45.4');
      const supabase = createClient(supabaseUrl, supabaseAnon, {
        auth: { flowType: 'pkce', autoRefreshToken: true, persistSession: true },
      });

      const errBox  = document.getElementById('err');
      const chkBox  = document.getElementById('check-email');
      const chkAddr = document.getElementById('check-email-addr');
      function showErr(msg) {
        errBox.textContent = msg;
        errBox.hidden = false;
        chkBox.hidden = true;
      }
      function showCheck(email) {
        chkAddr.textContent = email;
        chkBox.hidden = false;
        errBox.hidden = true;
      }

      document.querySelectorAll('button.oauth').forEach((btn) => {
        btn.addEventListener('click', async (e) => {
          e.preventDefault();
          const provider = btn.getAttribute('data-provider');
          const { error } = await supabase.auth.signInWithOAuth({
            provider,
            options: {
              redirectTo: `${location.origin}/api/auth/callback?next=${encodeURIComponent(nextParam)}`,
            },
          });
          if (error) showErr(`${provider} sign-in failed: ${error.message}`);
        });
      });

      const form = document.getElementById('magic-form');
      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        const email = new FormData(form).get('email');
        if (!email) return;
        const { error } = await supabase.auth.signInWithOtp({
          email,
          options: {
            emailRedirectTo: `${location.origin}/api/auth/callback?next=${encodeURIComponent(nextParam)}`,
          },
        });
        if (error) showErr(`Sign-in email failed: ${error.message}`);
        else showCheck(email);
      });
    }
  </script>

  <style>
    main.split {
      display: grid;
      grid-template-columns: 1fr 1fr;
      min-height: 100vh;
      background: #fff;
    }
    aside.left {
      background: #f6f6f5;
      padding: 3rem 3rem 4rem;
      position: relative;
      overflow: hidden;
    }
    aside .brand { display: inline-flex; align-items: center; color: #111; }
    /* Vertical auto-scrolling marquee, 2 columns. The card list is
       duplicated in markup so `translateY(-50%)` at the animation
       endpoint wraps seamlessly to the top. Slight per-column speed
       difference gives the layout an organic staggered feel. Mask
       fades top + bottom so cards "appear" from behind the edge
       instead of popping in.

       Respects prefers-reduced-motion: freezes at frame 0 so users
       with vestibular sensitivity see a static wall of cards. */
    .marquee {
      margin-top: 2.5rem;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      max-width: 44rem;
      /* Container height caps at viewport minus the brand row + padding.
         min-height keeps two full cards visible even on short screens. */
      height: calc(100vh - 10rem);
      min-height: 34rem;
      overflow: hidden;
      -webkit-mask-image: linear-gradient(to bottom, transparent 0, #000 12%, #000 88%, transparent 100%);
              mask-image: linear-gradient(to bottom, transparent 0, #000 12%, #000 88%, transparent 100%);
    }
    .marquee .col {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      will-change: transform;
    }
    .marquee .col-a { animation: aegis-marquee-up 40s linear infinite; }
    .marquee .col-b { animation: aegis-marquee-up 48s linear infinite; }

    @keyframes aegis-marquee-up {
      from { transform: translateY(0); }
      /* Cards are duplicated 2x in markup, so the loop endpoint is
         -50% of the total height — that's exactly one full cycle,
         and the second copy lands seamlessly where the first began. */
      to   { transform: translateY(-50%); }
    }

    @media (prefers-reduced-motion: reduce) {
      .marquee .col-a, .marquee .col-b { animation: none; }
    }
    .card {
      background: #fff;
      border: 1px solid #ececec;
      border-radius: 12px;
      padding: 1.75rem 1.25rem 1rem;
      margin: 0;
      position: relative;
    }
    .card .mark { position: absolute; top: 0.5rem; left: 1rem; color: #111; font-size: 2rem; line-height: 1; font-weight: 700; opacity: 0.85; }
    .card p { margin: 0 0 1rem; color: #333; font-size: 0.95rem; line-height: 1.5; }
    .card p em { font-style: italic; color: #E85E14; }
    .card footer { display: flex; align-items: center; gap: 0.75rem; }
    .card .avatar {
      width: 40px; height: 40px; border-radius: 50%;
      background: linear-gradient(135deg, #E85E14, #7a2f00);
      display: inline-flex; align-items: center; justify-content: center;
      color: #fff; font-weight: 600; font-size: 0.85rem;
    }
    .card .who { font-weight: 600; color: #111; font-size: 0.85rem; }
    .card .role { color: #666; font-size: 0.78rem; }

    section.right {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 3rem 2rem;
    }
    .form-wrap { width: 100%; max-width: 26rem; }
    .form-wrap h1 { font-size: 1.6rem; font-weight: 600; text-align: center; margin: 0 0 0.5rem; color: #111; }
    .form-wrap .sub { color: #666; text-align: center; margin: 0 0 2rem; font-size: 0.9rem; }
    .notice {
      background: #fff8ea; border: 1px solid #f0d68a; color: #6a4b00;
      padding: 0.75rem 1rem; border-radius: 8px; font-size: 0.85rem;
      margin-bottom: 1.5rem;
    }
    .notice a { color: #6a4b00; text-decoration: underline; }
    .notice code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 0.8rem; background: rgba(0,0,0,0.05); padding: 0 0.25rem; border-radius: 3px; }
    form label { display: block; margin-bottom: 1rem; }
    form label span { display: block; color: #111; font-size: 0.85rem; margin-bottom: 0.35rem; font-weight: 500; }
    form input {
      width: 100%; padding: 0.65rem 0.85rem;
      border: 1px solid #d4d4d4; border-radius: 8px; font-size: 0.95rem;
      background: #fff; color: #111;
      transition: border-color 0.12s;
    }
    form input:focus { outline: none; border-color: #111; box-shadow: 0 0 0 3px rgba(0,0,0,0.05); }

    .btn {
      display: inline-flex; align-items: center; justify-content: center; gap: 0.6rem;
      width: 100%; padding: 0.65rem 1rem;
      border-radius: 8px; font-weight: 500; font-size: 0.9rem;
      cursor: pointer; transition: background 0.12s, border-color 0.12s;
      margin-bottom: 0.6rem;
    }
    .btn.primary { background: #111; color: #fff; border: 1px solid #111; margin-bottom: 1.25rem; }
    .btn.primary:hover:not([disabled]) { background: #333; }
    .btn.oauth { background: #fff; color: #111; border: 1px solid #d4d4d4; }
    .btn.oauth:hover:not([disabled]) { background: #f8f8f8; }
    .btn[disabled] { opacity: 0.5; cursor: not-allowed; }

    .divider {
      display: flex; align-items: center; margin: 1.25rem 0;
      color: #999; font-size: 0.75rem;
    }
    .divider::before, .divider::after { content: ''; flex: 1; height: 1px; background: #ececec; }
    .divider span { padding: 0 0.75rem; text-transform: uppercase; letter-spacing: 0.1em; }

    .check-email {
      background: #f0f8f4; border: 1px solid #b0d8bd; color: #2d6b47;
      padding: 0.75rem 1rem; border-radius: 8px; font-size: 0.85rem;
      margin: 1rem 0 0;
    }
    .err {
      background: #fdf1f1; border: 1px solid #e8b0b0; color: #8a2929;
      padding: 0.75rem 1rem; border-radius: 8px; font-size: 0.85rem;
      margin: 1rem 0 0;
    }

    .tos { text-align: center; color: #999; font-size: 0.78rem; margin: 2rem 0 0.5rem; }
    .tos a { color: #666; text-decoration: underline; }
    .signup { text-align: center; color: #666; font-size: 0.85rem; margin: 0.5rem 0 0; }
    .signup a { color: #111; font-weight: 500; text-decoration: none; }
    .signup a:hover { text-decoration: underline; }

    @media (max-width: 900px) {
      main.split { grid-template-columns: 1fr; }
      aside.left { display: none; }
      section.right { padding: 4rem 1.5rem; min-height: 100vh; }
    }
  </style>
</Layout>
